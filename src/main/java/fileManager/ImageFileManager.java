package fileManager;

import java.io.File;
import java.awt.Image;


public class ImageFileManager extends IFileManager {


  public Image importFile(File file) {
    // 画像ファイルを読み込む処理を実装する

    return null; // 読み込んだ画像を返す
  }

  public Image exportFile(File file, Image image) {
    // 画像ファイルを書き込む処理を実装する

    return null; // 書き込んだ画像を返す
  }
}
